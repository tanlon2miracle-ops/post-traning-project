# TASK-001: 数据清洗管道开发与 H20 集群基建

> **状态 (Status)**: 📝 待认领 (Open)
> **认领人 (Assignee)**: @[请在此处@你的名字]
> **关联文档**: `docs/tech-stack.md`, `docs/decisions.md`

## 🎯 任务背景
在规模化数据合成前，我们需要准备好存量数据的清洗映射脚本。同时，硬件资源已就位，必须并行完成底层模型训练与推理环境的搭建。

## 📋 任务清单

- [ ] **行动 1：存量数据清洗与标签映射脚本**
  - **依赖**：需等待 Ops 组提交 `samples_v1.jsonl` 后获取数据结构。
  - **开发**：编写 Python 脚本，使用 Data-Juicer 或原生 Pandas，将原始多标签映射为 5 个一级标准标签。
  - **安全机制**：在脚本中加入防呆逻辑，如遇到未定义的异常标签需做日志记录或直接丢弃。
  - **交付动作**：在 `02-dev-workspace/data_pipeline/` 提交 `clean_and_map.py`。

- [ ] **行动 2：H20 集群训练环境初始化**
  - **动作**：在服务器上搭建 Python 虚拟环境/Docker 容器。
  - **依赖安装**：核心安装 LLaMA-Factory（支持 VL 多模态）、vLLM 和 Data-Juicer。
  - **模型准备**：下载 `Qwen2.5-VL-7B` 基础模型权重。
  - **交付动作**：在 `02-dev-workspace/envs/` 下提交 `requirements.txt` 或 `Dockerfile`，并附带一份极简的 `README.md` 说明环境激活与模型路径。

## 📤 验收标准
- 清洗脚本能在 Ops 提供的样例上无报错跑通，且环境文档能指导其他同学 5 分钟内拉起训练环境。
